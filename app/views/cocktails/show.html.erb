<nav class="navbar navbar-dark bg-dark" style="color: white;">
  <h1>Mister Cocktail:</h1>
</nav>

<div class="container">
  <div class="card-category mt-5" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('')">
  <%= @cocktail.name %>
 </div>
 <div class="info mt-5">
    <% @cocktail.doses.each do |dose| %>
    <li><%= dose.ingredient.name %></p>
    <p><%= dose.description%></p>
    <%= link_to "Delete",
                cocktail_dose_path(dose),
                method: :delete,
                data: { confirm: "Are you sure?" } %>
    <% end  %>
  </div>


  <p><%= link_to 'Back to cocktails', cocktails_path  %></p>
  <p><%= link_to 'Add a dose', new_cocktail_dose_path(@cocktail)  %></p>
</div>
